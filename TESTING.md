# 测试记录

当前处于开发验证阶段。尚未通过的条目不能视为已完成，也不能据此宣布正式发布。

## 自动化覆盖

`tests/smoke.mjs` 启动真实 Electron 应用，覆盖：

- PDF 渲染、文字层、翻页、缩放、位置恢复。
- EPUB ruby、横竖排切换、排版设置恢复。
- 当前页英文 OCR、文字复制、TXT 导出、取消。
- 深浅色主题保存。
- 渲染进程没有 Node.js 入口，外网请求阻断。

2026-09-24：源码运行与打包后的 Windows x64 EXE 均通过上述回归。测试窗口保持隐藏。

## 已修复的启动问题

- 离线请求过滤器误拦截 `net.fetch(file://...)`：改为受限的本地资源读取服务，HTML/JS/CSS/WASM 类型明确。
- Electron 沙箱子进程缺少运行时文件读取权限：启动前准备程序文件的 AppContainer 读取/执行权限，保留 Chromium 沙箱。便携阅读数据不在授权范围内。
- 启动失败记录一次错误并退出，不自动重启。测试窗口隐藏；截图接口在无图形桌面下可能失败，不据此判断应用崩溃。
- EPUB 页面挂载前调用尺寸调整：等待排版启动，并仅对已经挂载的页面调整尺寸。

`tests/startup.test.cjs`、`tests/runtime.test.cjs` 均通过。已观察到启动资源回归先失败、修复后通过；真实隐藏窗口启动和阅读测试随后通过。

## OCR 数据与视觉

- 五份语言数据及许可证已与 tessdata_fast 固定提交 `87416418657359cb625c412a48b6e1d6d41c29bd` 的 Git blob 哈希核对一致，同时保存 SHA-256。
- 英文、简中、繁中合成单行样本曾获得 100% 归一化文本相似度；日文横排在选择单行布局后为 100%，日文竖排为 85.7%。这不是通用准确率评估，竖排仍存在错字。
- 首页、深色首页、PDF 与日文竖排页完成无界面浏览器截图检查；其中桌面文件接口为测试替身，视觉截图不作为原生集成证据。

## 发行验收仍需记录

- 中日英真实扫描件、低清晰度和复杂版式的 OCR；合成样本不能替代这些验收。
- EPUB 长篇位置恢复、目录跳转与真实出版物。
- 更多 PNG/JPEG 尺寸、旋转及损坏图像；基础打开、缩放和 OCR 已通过。
- 损坏文档、模型缺失、关闭文档或取消后的任务状态。
- 未安装开发环境的干净 Windows 机器测试；当前机器的打包后二进制回归已通过。
- Windows 10/11、高 DPI、键盘、屏幕阅读器。
- GitHub CI、源码归档和发行文件哈希；许可证文本与模型哈希已整理。

当前机器上的自动化运行不能替代干净机器兼容性验收。
