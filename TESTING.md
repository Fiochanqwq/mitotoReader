# 测试记录

当前处于开发验证阶段。尚未通过的条目不能视为已完成，也不能据此宣布正式发布。

## 2026-09-24 AI、OCR 与连续阅读改动

- `npm test`：16 项 Node 用例及原有阅读器、书库、长 EPUB 集成回归通过；新增工作台集成验证通过，覆盖连续滚动、最多五页画布、滚轮翻页、预设、AI 任务切换、缓存隔离、取消与核对提示。
- `tests/ocr-languages.mjs`：12 种语言模式通过。新增韩语、德语、法语、西班牙语、俄语、意大利语、葡萄牙语清晰合成样本的归一化相似度均为 100%。日文竖排仍为 85.7%，其余既有样本为 100%；不代表真实扫描件准确率。
- `tests/python-bootstrap.cjs`：实际下载官方固定哈希 Python 包，创建无 pip 虚拟环境，再运行 ensurepip，最终 `pip --version` 成功。与系统 Anaconda 隔离；本机 Anaconda 的 `html` 标准库依赖缺失会使原安装流程失败。
- `tests/engine-install.cjs mineru`：在隔离测试目录实际完成 MinerU 4.x 和依赖安装，`mineru-kit --help` 成功并写入就绪标记。未在此轮运行大型模型解析；首次模型下载仍受模型源网络和硬件条件影响。
- AI 服务端协议使用模拟响应测试，未调用付费模型。已验证六家适配器的指令层次、任务提示词、截断拒收、取消、数字与引文检查、无损分段；未据此宣称真实翻译准确率。
- 工作台截图使用模拟模型输出做排版检查；正常隐藏窗口测试不强制截图，以免无桌面渲染造成截图超时。

网络安装专项可运行 `node tests/python-bootstrap.cjs` 和 `node tests/engine-install.cjs mineru`。前者需要下载约 14 MB，后者会安装解析组件与依赖，需较多磁盘空间；首次模型解析还需另行下载模型。

## 2026-09-24 本轮功能验证

本轮对 Windows x64 便携版及相关数据逻辑完成以下自动化验证，均使用独立测试目录，不写入正常阅读记录：

- 6 项 Node 测试：旧版书库迁移、内容去重与文件变化检测、窗口适配与渲染像素预算、批量页码边界、运行时权限边界、离线资源启动。
- `tests/smoke.mjs`：PDF 渲染 / 文字 / 位置、EPUB 注音 / 方向 / 设置、英文 OCR、复制导出、缓存命中、两页批量、框选识别、取消、识别中返回书库、主题及外网隔离。
- `tests/features.mjs`：窗口缩放 / 全屏、全文搜索与结果跳转、书签导航、输入框快捷键隔离、书库元数据 / 搜索 / 去重 / 持久化、文件移动重新定位、EPUB 搜索、损坏 PDF 恢复、移除记录不删除原件。
- `tests/long-epub.mjs`：160 个长段落的合成 EPUB，翻页后缩放窗口保留 CFI，重开后恢复进度与书签。
- `tests/ocr-languages.mjs`：五种语言模式通过；英文、简中、繁中、日文横排合成样本归一化相似度 100%，日文竖排 85.7%。这些数值不能解释为真实扫描件准确率。
- `tests/visual.mjs`：1280×860 与 650×520 的首页、PDF、EPUB 截图检查；浏览器宿主接口为测试替身，不能代替原生集成测试。

新增用例曾实际发现并修复 PDF.js 6 文档销毁接口不兼容导致的返回书库失败。OCR 在本机内存紧张时曾发生内存分配失败；改为最多 400 万像素、默认 200 DPI 后，串行运行完整 OCR 回归通过。该像素上限是资源约束，不代表所有机器上的峰值内存保证。

`npm test` 已包含 Node、阅读 / OCR、书库、长篇 EPUB 测试；CI 同样增加上述检查。发布工作流在 Windows 托管环境完成便携版回归、安装 EXE 构建与安装后功能回归，再生成 ZIP、源码与 SHA-256 校验文件；所有步骤通过后才公开 Release。具体运行结果见 GitHub Actions。真实扫描件准确率、复杂出版物、Windows 10 和屏幕阅读器仍需后续验收。

## 自动化覆盖

`tests/smoke.mjs` 启动真实 Electron 应用，覆盖：

- PDF 渲染、文字层、翻页、缩放、位置恢复。
- EPUB ruby、横竖排切换、排版设置恢复。
- 当前页英文 OCR、文字复制、TXT 导出、取消。
- 深浅色主题保存。
- 渲染进程没有 Node.js 入口，外网请求阻断。

2026-09-24：源码运行与打包后的 Windows x64 EXE 均通过上述回归。测试窗口保持隐藏。

## 已修复的启动问题

- 离线请求过滤器误拦截 `net.fetch(file://...)`：改为受限的本地资源读取服务，HTML/JS/CSS/WASM 类型明确。
- Electron 沙箱子进程缺少运行时文件读取权限：启动前准备程序文件的 AppContainer 读取/执行权限，保留 Chromium 沙箱。便携阅读数据不在授权范围内。
- 启动失败记录一次错误并退出，不自动重启。测试窗口隐藏；截图接口在无图形桌面下可能失败，不据此判断应用崩溃。
- EPUB 页面挂载前调用尺寸调整：等待排版启动，并仅对已经挂载的页面调整尺寸。

`tests/startup.test.cjs`、`tests/runtime.test.cjs` 均通过。已观察到启动资源回归先失败、修复后通过；真实隐藏窗口启动和阅读测试随后通过。

## OCR 数据与视觉

- 五份语言数据及许可证已与 tessdata_fast 固定提交 `87416418657359cb625c412a48b6e1d6d41c29bd` 的 Git blob 哈希核对一致，同时保存 SHA-256。
- 英文、简中、繁中合成单行样本曾获得 100% 归一化文本相似度；日文横排在选择单行布局后为 100%，日文竖排为 85.7%。这不是通用准确率评估，竖排仍存在错字。
- 首页、深色首页、PDF 与日文竖排页完成无界面浏览器截图检查；其中桌面文件接口为测试替身，视觉截图不作为原生集成证据。

## 发行验收仍需记录

- 中日英真实扫描件、低清晰度和复杂版式的 OCR；合成样本不能替代这些验收。
- EPUB 长篇位置恢复、目录跳转与真实出版物。
- 更多 PNG/JPEG 尺寸、旋转及损坏图像；基础打开、缩放和 OCR 已通过。
- 损坏文档、模型缺失、关闭文档或取消后的任务状态。
- 未安装开发环境的干净 Windows 机器测试；当前机器的打包后二进制回归已通过。
- Windows 10/11、高 DPI、键盘、屏幕阅读器。
- GitHub CI、源码归档和发行文件哈希；许可证文本与模型哈希已整理。

当前机器上的自动化运行不能替代干净机器兼容性验收。
